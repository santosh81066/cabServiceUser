fastapi>=0.65.0,<0.68.0
uvicorn[standard]>=0.15.0
pydantic>=1.8.0,<2.0.0
email-validator>=1.0.0
python-dotenv>=1.0.0
python-multipart>=0.0.5
firebase-admin>=6.0.0

# Optional: for sending real OTP SMS (install if you use that provider)
# twilio          # global SMS (set TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN, TWILIO_PHONE_NUMBER)
# python-gammu    # free & open source: USB GSM modem + SIM (set GAMMU_DEVICE e.g. /dev/ttyUSB0)
# no extra deps   # MSG91 uses stdlib urllib (set MSG91_AUTH_KEY)
